// this is index.js


const getUrl = "http://localhost:3000/missions/"
const postUrl = "http://localhost:3000/assigned/"





function getSixMissions() {

}
// check whether user already has missions in collection
// if so, display them
// if not (or if they click the 'get new missions' button), 
// get 6 missions from the library:
// get all missions into a global array
// shuffle the array
// then take the first six
